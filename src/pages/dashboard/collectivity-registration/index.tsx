import EditorComponent from '../editor-component'

const CollectivityRegistration: React.FC = () => {
  return (
    <EditorComponent
      title={'COLLECTIVITY REGISTRATION'}
      handleReadOnly={() => null}
      handleSave={() => null}
      handleTextChange={() => null}
      isReadOnly
      content=""
    />
  )
}

export default CollectivityRegistration
